<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/search_toolbar"
    style="@style/ToolbarStyle"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light">

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/search_refresh_iv"
            style="@style/IconToolbarStyle"
            android:src="@drawable/ic_refresh"/>

        <TextView
            style="@style/TextToolbarDefaultStyle"
            android:text="@string/search_find_book"
            app:layout_constraintLeft_toRightOf="@id/search_refresh_iv"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <ImageView
            android:id="@+id/search_delete_iv"
            style="@style/IconToolbarStyle"
            android:layout_marginEnd="@dimen/margin_default"
            android:src="@drawable/ic_delete"/>
    </android.support.constraint.ConstraintLayout>
</android.support.v7.widget.Toolbar>